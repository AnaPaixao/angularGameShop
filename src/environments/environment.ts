// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiDados: "http://localhost:3000/",
  // For Firebase JS SDK v7.20.0 and later, measurementId is optional
  firebaseConfig: {
    apiKey: "AIzaSyDUZENwiNco3NmOWWcNSIW8z_uQqejBHUA",
    authDomain: "angulargameshop.firebaseapp.com",
    databaseURL: "https://angulargameshop.firebaseio.com",
    projectId: "angulargameshop",
    storageBucket: "angulargameshop.appspot.com",
    messagingSenderId: "562980989959",
    appId: "1:562980989959:web:dc5c7c8f65eb191f7c8ea2",
    measurementId: "G-KN0VMYTDWB"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
